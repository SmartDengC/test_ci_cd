<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dao.UserCodeDao">
    <select id="FindByUsername" parameterType="java.lang.String" resultType="com.pojo.User">
            select * from persistent_logins
            where username=#{username};
    </select>

    <select id="FindPermissionByUsername" parameterType="java.lang.String"  resultType="com.pojo.Permission">
        SELECT permission.*
        FROM sys_user user
        INNER JOIN sys_user_role user_role ON `user`.id=user_role.user_id
        INNER JOIN sys_role_permission role_permission ON user_role.role_id=role_permission.role_id
        INNER JOIN sys_permission permission ON role_permission.perm_id=permission.id
        WHERE username=#{username}

    </select>
    
    <select id="UserRegister" parameterType="java.lang.String" resultType="java.lang.Boolean">
        select count(*) from sys_user,user_invitecode where username=#{username}  and invite_code=#{invitecode} and email=#{email}
    </select>

    <insert id="SaveUserInformation" statementType="CALLABLE">
        {CALL insertUserInformation(
        #{username,mode=IN,jdbcType=VARCHAR},
        #{password,mode=IN,jdbcType=VARCHAR},
        #{email,mode=IN,jdbcType=VARCHAR},
        #{createDate,mode=IN,jdbcType=VARCHAR},
        #{invitecode,mode=IN,jdbcType=VARCHAR}
        )}
    </insert>

    <select id="CheckAccountChange" resultType="java.lang.Integer">
        select count(*) from sys_user where username=#{username} and `password`=#{password} and email=#{email}
    </select>

    <select id="CheckAccountFind" resultType="java.lang.Integer">
       select count(*) from sys_user where username=#{username} and email=#{email}
    </select>


    <select id="CheckCode" resultType="java.lang.Integer">
        select count(*) from user_code
        INNER JOIN sys_user ON  `sys_user`.id =user_code.user_id
        WHERE username = #{username} and code = #{code}
    </select>

    <update id="UpdatePassword" >
        update sys_user set password=#{password} where username=#{username}
    </update>

</mapper>